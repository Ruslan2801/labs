#include <iostream>
#include <algorithm>


int main()

{
    

    //lab 3 v1
    /*int k = -1;
    int p = 1;
    int min = INT_MAX;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        if (x % 10 == 2 || x % 10 == 4)
        {
            p *= x;
            if (min > x)
            {
                min = x;
                k = i;
            }

        }

    }
    std::cout << p << std::endl;
    std::cout << min << std::endl;
    std::cout << k << std::endl;*/

    


    //v2
    /*int k = -1;
    int max = INT_MIN;
    int s = 0;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        if (x > -2 && x < 19)
        {
            s += x;
            if (max < x)
            {
                max = x;
                k = i;
            }
        }
    }
    std::cout << s << std::endl;
    std::cout << max << std::endl;
    std::cout << k << std::endl;*/


    //v3
    /*int min = INT_MAX;
    int k = -1;
    int p = 1;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        if (x > -2 && x < 20)
        {
            p *= x;
            if (min > x)
            {
                min = x;
                k = i;
            }
        }
    }
    std::cout << p << std::endl;
    std::cout << min << std::endl;
    std::cout << k << std::endl;*/



    // v4
    /*int k = -1;
    int max = INT_MIN;
    int s = 0;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        if (x % 10 == 0 || x % 10 == 7)
        {
            s += x;
            if (max < x)
            {
                max = x;
                k = i;
            }
        }
    }
    std::cout << s << std::endl;
    std::cout << max << std::endl;
    std::cout << k << std::endl;*/


    // v5
    /*int k = -1;
    int min = INT_MIN;
    int p = 1;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        {
            p *= x;
            if (min < x)
            {
                min = x;
                k = i;
            }
        }
    }
    std::cout << p << std::endl;
    std::cout << min << std::endl;
    std::cout << k << std::endl; */


    //6
    /*int k = -1;
    int min = INT_MIN;
    int s = 1;
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        std::cin >> x;
        {
            s += x;
            if (min < x)
            {
                min = x;
                k = i;
            }
        }
    }
    std::cout << s << std::endl;
    std::cout << min << std::endl;
    std::cout << k << std::endl; */
    
    
    /*int x, a, b, c, d;
    std::cin >> x >> a >> b >> c >> d;
    std::cout << x%2 << a%2 << b%2 << c%2 << d%2;*/
    



    /*int num;
    double sq_root;
    for (num = 1; num < 100; num++) {
        sq_root = sqrt((double)num);
        std::cout << num << " " << sq_root << '\n';
    }
    return 0;*/


    
    

}